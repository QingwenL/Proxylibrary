ext {

    // 用于编译的SDK版本
    COMPILE_SDK_VERSION = 25
    // 用于Gradle编译项目的工具版本
    BUILD_TOOLS_VERSION = '25.0.2'
    // 最低支持Android版本
    MIN_SDK_VERSION = 19
    // 目标版本
    TARGET_SDK_VERSION = 25
    // 版本标识
    VERSIONCODE  =  2
    // 版本号
    VERSIONNAME  = '1.0.1'

    // 设置是否使用混淆
    MINIFY_ENABLED = false
    MINIFY_DISABLED = false

    // APP 包名
    APPLICATION_ID = 'com.app.sample'


    // 引用第三方库版本
    //appcompat-v7
    APPCOMPAT_VERSION = '25.1.0'
    //support-v4
    SUPPORT_VERSION = '25.1.0'



}